# This file is a shared example for AI provider settings.
#
# For the FastAPI backend, prefer using `backend/.env.example` → `backend/.env`.
# If we later add Node-based tooling that also calls LLM providers, it can reuse these keys.

# Model provider selection:
# - none
# - openai-compatible
MODEL_PROVIDER=openai-compatible

# For openai-compatible provider
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=your_key_here
OPENAI_MODEL=gpt-4o-mini

# Optional: used as fallback placeholder fill
DEFAULT_LANGUAGE=ru
